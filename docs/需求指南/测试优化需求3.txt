导入规则页还是不太对，比如规则项，需要先让用户选择excel行，读取对应行的所有字段名，然后用户选择需要的规则项字段，最后按照字段名配置各个字段对应的列
还有导出配置，也需要先让用户选择excel行，读取对应行的所有字段名，然后用户选择需要的导出项字段，最后按照字段名配置各个字段对应的列
先用的导入规则模板，解析出的页面有点问题，字段映射不需要，按照上面的设计来映射
然后是导出配置：应该从用户配置的规则项字段和导出项字段中选，第一先选择需要导出的所有字段，第二然后用规则项字段和导出项字段进行相应的条件设置和聚合设置
这个再和新建配置项保持一致，帮我重新优化下